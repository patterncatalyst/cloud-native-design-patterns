syntax = "proto3";

package inventory;

service Inventory {
  rpc ReserveStock (ReserveRequest) returns (ReserveReply);
}

message ReserveRequest {
  string sku = 1;
  int32 quantity = 2;
}

message ReserveReply {
  bool reserved = 1;
  int32 remaining = 2;
}
